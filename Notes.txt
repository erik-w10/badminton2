Bein met (met write toegang op node installatie in /opt/node...):

    npm install -g @vue/cli

    vue create badminton2

    cd badminton2

    vue add electron-builder

Picked latest version.  Development server can started with:

    npm run electron:serve

=> Runs browser in debug view, toggle with ctr-shift-I (or menu)
Krijg wel foutmelding (het schijnt dat dit eigenlij niet werkt in een VM):

    libva error: vaGetDriverNameByIndex() failed with unknown libva error, driver_name = (null)
 
