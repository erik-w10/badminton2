URLS
VUE tutorial https://vuejs.org/tutorial/#step-1
VUE draggable next https://github.com/SortableJS/vue.draggable.next



Bein met (met write toegang op node installatie in /opt/node...):

    npm install -g @vue/cli

    vue create badminton2

    cd badminton2

    vue add electron-builder

Picked latest version.  Development server can started with:

    npm run electron:serve

=> Runs browser in debug view, toggle with ctr-shift-I (or menu)
Krijg wel foutmelding (het schijnt dat dit eigenlij niet werkt in een VM):

    libva error: vaGetDriverNameByIndex() failed with unknown libva error, driver_name = (null)
 
=> Git initial commit

    npm install nfc-pcsc
    (13 vulnerabilities)
    npm 
    (not steeds 13)

(electron:serve doet het nog)

    npm install vuedraggable
    npm install file-dialog

Commented out use of node 'fs' for export (not implemented for now)
Fixed mix of v-for / v-if error (2x)
Fixed unused variable errors

    npm install path-browserify
    npm install xlsx-parse-json
    npm install fs --save

Not sure where the Electron window creation code when (probably createApp())
->  https://nklayman.github.io/vue-cli-plugin-electron-builder/
Found features to enable "Node integration" in Electron and nested in Vue config at vue.config.js
Now stuck with runtime error "Cannot find module '../js/chunk-vendors.js'"
Removed target node from vue.config.js module.exports.configureWebpack
Removed entire module.exports.configureWebpack = {
  resolve: {
    fallback: {"path" : require.resolve("path-browserify")}
  }
};

// Fully disabled the NFC package for now (commented out, want to see that problems are local to it)
-> crash in draggable due to change in slot/scoped-slot (VUE 3.0 incompatibility)
In pakcage.json deleted     "vuedraggable": "^2.24.3" (VUE 2.0 only)

    npm install vuedraggable@next

=> Some output on HTML screen (but errors in draggable)
Will read draggalbe@next docs... (ruuugggh!)